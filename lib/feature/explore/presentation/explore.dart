

import 'package:flutter/material.dart';

import '../../../core/common/widget/nav_bar/bottom_bar_screen.dart';

class ExploreScreen extends StatelessWidget {
  const ExploreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("explore"),
    );
  }
}
